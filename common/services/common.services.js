(function () {
    "use strict";

    angular.module("common.services",
                  ["ngResource"])
                  //$resource in inside the ngResource Module so we need to inject that into this module since we wanna use the $resource
            
})();